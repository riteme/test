#include <cstdio>
#include <cstring>
#include <cstdlib>
#include <vector>
#include <algorithm>

using namespace std;

#define NMAX 100000

static void initialize() {}

int main() {
    // freopen("network_tenderRun.in", "r", stdin);
    // freopen("network_tenderRun.out", "w", stdout);
    initialize();

    fclose(stdin);
    fclose(stdout);
    return 0;
}  // function main
